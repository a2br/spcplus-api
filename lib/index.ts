/**
 * import Sg from "../spcgg-api";
 *
 * Sg.config({ host: "https://example.com" });
 *
 * const client = new Sg.Session("usr", "pwd");
 * const login = await client.login();
 * const snapshot = await client.takeSnapshot();
 * const snapshots = await client.snapshots();
 * const clubs = await client.clubs();
 * const menu = await client.menu({ date?: Date });
 * client.logout();
 *
 *
 * or stuff like that
 */
